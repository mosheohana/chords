services:
  - type: web
    name: chordlab-api
    env: python
    plan: free
    buildCommand: pip install Cython==3.0.11 numpy==1.26.4 && pip install -r requirements.txt
    startCommand: python server.py $PORT
    healthCheckPath: /api/health
